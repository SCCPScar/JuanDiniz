PORT=3000
JWT_SECRET=escolha-um-segredo-longo-e-aleatorio-aqui
NODE_ENV=development

# Conexão com o MySQL (o mesmo banco visível no phpMyAdmin).
# Valores padrão do XAMPP/WAMP em Windows: host localhost, usuário root, senha vazia.
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=barbearia_odin
